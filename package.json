{
  "name": "hardhat-package",
  "version": "1.0.0",
  "description": "Hardhat contracts for Time Capsule",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network alfajores",
    "deploy:localhost": "hardhat run scripts/deploy.ts --network localhost",
    "node": "hardhat node",
    "test-rpc": "hardhat run scripts/test-rpc.ts --network alfajores",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@typechain/ethers-v6": "^0.5.1",
    "@typechain/hardhat": "^9.1.0",
    "hardhat": "^2.19.4",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}

